# Local Wiki

This is the writable wiki managed by Wiki Server. Markdown files remain local
and the directory has its own Git history.

Start from `index.md`. You can open this directory as an Obsidian vault or use
any editor that works with plain Markdown.
